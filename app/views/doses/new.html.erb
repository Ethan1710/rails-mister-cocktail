
<%= simple_form_for [@cocktail, @dose] do |d| %>
 <%= d.association :ingredient %>
 <%= d.input :description %>


  <!-- d.input :rating, collection: 0..5  -->

  <%= d.submit %>
<% end %>

<%= link_to "Back to doses", cocktail_path(@cocktail), class: "btn btn-primary"  %>
